cmake_minimum_required(VERSION 3.24)
project(jevt_installed_consumer LANGUAGES CXX)
find_package(jevtpp CONFIG REQUIRED)
add_executable(consumer main.cpp)
target_link_libraries(consumer PRIVATE jevt::jevt)
enable_testing()
add_test(NAME installed_consumer COMMAND consumer)
