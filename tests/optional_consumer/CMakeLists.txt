cmake_minimum_required(VERSION 3.24)
project(jevt_optional_installed_consumer LANGUAGES CXX)
find_package(jevtpp CONFIG REQUIRED)
add_executable(optional_consumer main.cpp)
target_link_libraries(optional_consumer PRIVATE jevt::remote jevt::asio)
enable_testing()
add_test(NAME optional_installed_consumer COMMAND optional_consumer)
